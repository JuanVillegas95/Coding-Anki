import styled, { keyframes, css } from 'styled-components';
import { ButtonInput, CenterDiv } from "@/utils/style.base"
import { LoginContainerDiv } from "@/utils/style.login"

export const HomeButtonInput = styled(ButtonInput).attrs({ value: "Next" })``
export const HomeWrapperDiv = styled(CenterDiv)``;
export const HomeContainerDiv = styled(LoginContainerDiv)``;
